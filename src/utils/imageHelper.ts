/**
 * Returns empty string when no system/ERP image is available,
 * allowing the UI to display the store logo instead of Unsplash photos.
 */
export function getSmartProductImage(productName: string = '', categoryName: string = ''): string {
  return '';
}

